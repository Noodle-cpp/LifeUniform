@page
@model LifeUniform.Web.Pages.Account.AuthModel
@{
    ViewData["Title"] = Model.Mode == "register" ? "Регистрация" : "Авторизация";
}

<div class="container py-5 text-center">
    <p class="text-muted mb-0">Открываем форму…</p>
</div>
